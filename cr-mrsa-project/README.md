# Mini RSA Project (cr-mrsa-project)

64비트 Mini RSA 알고리즘을 C 언어로 구현한 암호학 프로젝트입니다.  
실제 RSA(2048bit)보다 작은 버전이지만,  
**키 생성 → 암호화 → 복호화**의 흐름 전체를 직접 구현하여  
RSA 알고리즘의 동작 원리를 이해하는 데 초점을 맞춘 프로젝트입니다.

---

## 🔑 프로젝트 목표

- 64비트 RSA 모듈러스 \(2^63 ≤ n < 2^64\) 생성  
- Carmichael 함수 λ(n)을 이용해 공개키·개인키 생성  
- Miller–Rabin 소수 판별로 32bit 소수 p, q 생성  
- `m^k mod n` 방식의 암·복호화 구현  

사용자가 실제 호출하는 인터페이스는 다음과 같습니다:

```c
void mRSA_generate_key(uint64_t *e, uint64_t *d, uint64_t *n);
int  mRSA_cipher(uint64_t *m, uint64_t k, uint64_t n);
```

---

## 🧩 핵심 기능

| 기능 | 설명 |
|------|------|
| **키 생성** | Miller–Rabin으로 32비트 소수 p, q 생성 → n = p·q 구성 |
| **Carmichael 함수** | λ(n) = lcm(p−1, q−1) 기반으로 e, d 계산 |
| **모듈러 연산** | mod_add, mod_mul, mod_pow, gcd, umul_inv 직접 구현 |
| **소수 판별** | 64bit 범위에서 12개 기저를 활용한 결정적 Miller–Rabin |
| **암·복호화** | mRSA_cipher()로 m^k mod n 수행 |
| **랜덤 테스트** | 제공된 테스트 코드 기준 Random testing PASS |

---

## 🛠 사용 기술 스택

- **언어:** C (C99)
- **난수:** arc4random, arc4random_uniform  
- **수론 알고리즘:**  
  - 유클리드 호제법  
  - 확장 유클리드 알고리즘  
  - 모듈러 거듭제곱  
  - 모듈러 역원 계산
- **환경:** Linux / macOS / WSL

---

## 📁 폴더 구조

```plaintext
cr-mrsa-project/
├── src/
│   ├── mRSA.c
│   └── test.c
├── include/
│   └── mRSA.h
├── result/
│   ├── expected_result.txt
│   └── my_result.txt
├── Makefile
└── README.md
```

---

## ⚙️ 빌드 & 실행

```bash
# 빌드
make

# 실행
./mRSA
```

실행 결과는 다음 파일에서 확인할 수 있습니다:

```
result/my_result.txt
```

여기에는 생성된 키(e, d, n)와  
여러 메시지 m에 대한 암·복호화 결과가 출력됩니다.

---

## 📌 프로젝트 의의

이 프로젝트는  
**암호학적 사고력 + 시스템 프로그래밍 능력 + 수론 알고리즘 구현 능력**을  
종합적으로 보여줄 수 있는 Mini RSA 구현 프로젝트입니다.

